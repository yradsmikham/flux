# not for execution; just sets some common variables up

REPO_ROOT=$(git rev-parse --show-toplevel)
: ${BASE:=$REPO_ROOT/build/test}

export GOPATH=$BASE/gopath
GOBASE=$BASE
export PATH=$BASE/bin:$BASE/go/bin:$GOPATH/bin:$PATH
